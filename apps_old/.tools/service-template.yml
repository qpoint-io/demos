  {{service_name}}:
    build:
      context: {{build_path}}
      args:
        - TOKEN=${TOKEN}
    environment:
      - HTTP_PROXY=${QPOINT_HTTP_PROXY}
      - HTTPS_PROXY=${QPOINT_HTTPS_PROXY}
    ports:
      - "{{port}}:80"
    depends_on:
      - qpoint.local
